services:
  - type: web
    name: inventario-medicamentos
    env: node
    region: ohio
    plan: free
    buildCommand: npm run build
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: DATABASE_URL
        scope: build
    autoDeploy: true
